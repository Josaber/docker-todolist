spring:
  datasource:
    url: jdbc:mysql://db:3306/TodoList?useUnicode=yes&characterEncoding=utf8&&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: false
    hibernate:
      ddl-auto: create-drop